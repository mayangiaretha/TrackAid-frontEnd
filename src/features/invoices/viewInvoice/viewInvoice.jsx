import MiniDrawer from '../../../layout/drawer/drawer';
import Invoice from './invoice';

const ViewInvoice = () => {
  return (
    <MiniDrawer>
      <Invoice />
    </MiniDrawer>
  );
};

export default ViewInvoice;
